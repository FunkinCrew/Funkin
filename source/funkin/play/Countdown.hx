package funkin.play;

enum abstract CountdownStep(String) from String to String
{
	var BEFORE = "BEFORE";
	var THREE = "THREE";
	var TWO = "TWO";
	var ONE = "ONE";
	var GO = "GO";
	var AFTER = "AFTER";
}
